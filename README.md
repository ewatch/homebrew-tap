# homebrew-tap

Homebrew tap for [snow-cli](https://github.com/ewatch/snow-cli).

## Usage

```sh
brew tap ewatch/tap
brew install snow-cli
```

## Formula

- [snow-cli](Formula/snow-cli.rb) — CLI gateway for LLMs and coding agents to access ServiceNow instances.
